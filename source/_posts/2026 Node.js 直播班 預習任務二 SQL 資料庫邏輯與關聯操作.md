---
title: 2026 Node.js 直播班 預習任務二 SQL 資料庫邏輯與關聯操作
date: 2026-06-19 10:11:28
tags: Practice
description: 主鍵、外來鍵、INNER JOIN 練習改寫版
---

[支援 postgreSQL 語法的練習網站: PGTutorial]( https://www.pgtutorial.com/playground/)

## Q1：拯救明華國小的資料庫，哪個欄位適合變成**外來鍵**？

<table>
  <tr>
    <th>學生編號</th>
    <th>姓名</th>
    <th>班級</th>
    <th>性別</th>
    <th>年齡</th>
  </tr>
  <tr>
    <td>1</td>
    <td>小明</td>
    <td>三年一班</td>
    <td>男</td>
    <td>8</td>
  </tr>
  <tr>
    <td>2</td>
    <td>小華</td>
    <td>三年二班</td>
    <td>女</td>
    <td>9</td>
  </tr>
  <tr>
    <td>3</td>
    <td>小美</td>
    <td>三年一班</td>
    <td>男</td>
    <td>8</td>
  </tr>
  <tr>
    <td>4</td>
    <td>小強</td>
    <td>三年一班</td>
    <td>女</td>
    <td>8</td>
  </tr>
   <tr>
    <td>5</td>
    <td>小智</td>
    <td>三年二班</td>
    <td>男</td>
    <td>9</td>
  </tr>
</table>

班級。因為班級與學生呈現**一對多**的關係，所以應該把班級拆成學生外來鍵。
>**班級的獨特性：**每個班級會有一個獨特的名稱或編號，用於標識不同的班級。
>**關聯性：**班級可以用來連接到一個包含班級詳細資訊的表，這樣可以實現資料的關聯性。

創建班級資料表
<font color=#0000FF> In [1]: </font>

```sql
CREATE TABLE class_list (
    id SERIAL PRIMARY KEY, --自動生成的唯一識別碼（使用 SERIAL 型別，會自動遞增），作為每個班級的主鍵（PRIMARY KEY）
    name VARCHAR(50) 
);
```

創建學生資料表
<font color=#0000FFF> In [2]: </font>

```sql
CREATE TABLE students (
    id SERIAL PRIMARY KEY, --自動生成的唯一識別碼，作為每個學生的主鍵。
    name VARCHAR(50),
    gender VARCHAR(10) CHECK (gender IN ('男', '女')), --儲存學生性別，CHECK 約束限制只能輸入 '男' 或 '女'。
    age INTEGER,
    class_id INTEGER, --儲存學生所屬班級的識別碼，參照 class_list 表格中的 id 欄位（外鍵關係）。
    FOREIGN KEY (class_id) REFERENCES class_list(id) -- 設定外來鍵關聯。
);
```

新增班級與學生資料
<font color=#0000FF> In [3]: </font>

```sql
BEGIN;

INSERT INTO class_list (name) VALUES --這部分表示要向class_list表中插入數據，並指定插入的欄位是name
    ('三年一班'),
    ('三年二班');

INSERT INTO students (name, age, gender, class_id) VALUES
    ('小明', 8, '男', 1),
    ('小華', 9, '女', 2),
    ('小美', 8, '男', 1),
    ('小強', 8, '女', 1),
    ('小智', 9, '男', 2);

COMMIT;
```
>將班級與學生的新增操作包在 `BEGIN; ... COMMIT;` 裡，確保兩張表的資料要嘛全部寫入、要嘛全部回滾，不留下不完整的狀態。

顯示數據
<font color=#0000FF> In [4]: </font>

```sql
SELECT 
    students.id AS 學生編號,
    students.name AS 姓名,
    class_list.name AS 班級,
    students.gender AS 性別,
    students.age AS 年齡    
FROM students
INNER JOIN class_list ON students.class_id = class_list.id;
```
>這個查詢會返回一個結果集，其中包含每個學生的編號、姓名、班級名稱、性別和年齡。這樣的查詢有助於獲取完整的學生和班級資訊，便於查看和分析。

<font color=#FF0000> Query Result [4]: </font>
![SELECT FROM Students](https://firebasestorage.googleapis.com/v0/b/pictureforu-3b8e9.appspot.com/o/2026Node-js-camp-week0-task-2-SQL-Qurety-Result%2F2026-week0-sql-01.png?alt=media&token=62090f03-5055-418a-a1d4-0fe978c3377d)

## Q2：第一題的延伸，多了一個*班級老師*

<table>
  <tr>
    <th>學生編號</th>
    <th>姓名</th>
    <th>班級</th>
    <th>班級老師</th>
    <th>性別</th>
    <th>年齡</th>
  </tr>
  <tr>
    <td>1</td>
    <td>小明</td>
    <td>三年一班</td>
    <td>廖洧杰</td>
    <td>男</td>
    <td>8</td>
  </tr>
  <tr>
    <td>2</td>
    <td>小華</td>
    <td>三年二班</td>
    <td>卡斯伯</td>
    <td>女</td>
    <td>9</td>
  </tr>
  <tr>
    <td>3</td>
    <td>小美</td>
    <td>三年一班</td>
    <td>查理</td>
    <td>男</td>
    <td>8</td>
  </tr>
  <tr>
    <td>4</td>
    <td>小強</td>
    <td>三年一班</td>
    <td>麥可</td>
    <td>女</td>
    <td>8</td>
  </tr>
   <tr>
    <td>5</td>
    <td>小智</td>
    <td>三年二班</td>
    <td>李燕容</td>
    <td>男</td>
    <td>9</td>
  </tr>
</table>

在設計資料庫時，是否將班級導師設為外來鍵取決於你的數據關聯需求。以下是一些考量：
#### 使用外來鍵的情況
##### 導師有獨立的表格：
如果導師有自己的表格（例如teachers表），並且你需要存儲導師的詳細資訊（如聯絡方式、專業等），那麼將teacher_id設為外來鍵是合適的。
##### 數據一致性：
使用外來鍵可以確保數據的完整性和一致性，防止班級中出現不存在的導師。
#### 不使用外來鍵的情況
##### 簡單的系統：
如果系統相對簡單，且導師資訊不需要獨立管理，可以直接在class_list表中使用teacher名稱欄位。
##### 靈活性：
不使用外來鍵可以增加靈活性，尤其是在導師變動頻繁或不需要嚴格關聯的情況下。


從表中可以觀察班級與班級老師呈現**一對多**關係(六角幼兒園?)，學生與老師則是**一對一**關係；故在此我們把老師資料拆成學生的外來鍵。

創建班級老師資料表
<font color=#0000FF> In [5]: </font>

```sql
CREATE TABLE teachers (
    id SERIAL PRIMARY KEY,     -- 老師編號，主鍵
    name VARCHAR(50)           -- 老師名稱
);
```
>id是這個表的第一個欄位，用來存儲老師的編號。
>SERIAL是一個數據類型，表示這個欄位會自動生成遞增的整數值，通常用於主鍵。
>PRIMARY KEY表示這個欄位是主鍵，每個值必須是唯一的，這樣可以唯一標識每一位老師。

將 teachers 欄位加入 students，並一次完成新增與更新
<font color=#0000FF> In [6]: </font>

```sql
BEGIN;

INSERT INTO teachers (name) VALUES --向teachers表的name欄位中插入數據。
    ('廖洧杰'),
    ('卡斯伯'),
    ('查理'),
    ('麥可'),
    ('李燕容');

ALTER TABLE students
ADD COLUMN teacher_id INTEGER,
ADD CONSTRAINT fk_teacher_id
FOREIGN KEY (teacher_id) REFERENCES teachers(id);

-- 此題每位學生的 teacher_id 恰好等於學生的 id，可合併為一行
UPDATE students SET teacher_id = id WHERE id BETWEEN 1 AND 5;

COMMIT;
```
>原本五行獨立的 `UPDATE` 改為一行：`teacher_id = id` 利用了這道題學生 id 與老師 id 一對一對應的特性。
>若對應關係不規則，可改用 `CASE`：
>```sql
>UPDATE students
>SET teacher_id = CASE id
>    WHEN 1 THEN 1  WHEN 2 THEN 2  WHEN 3 THEN 3
>    WHEN 4 THEN 4  WHEN 5 THEN 5
>END
>WHERE id BETWEEN 1 AND 5;
>```

使用INNER JOIN 合併資料表查詢學生與班級導師的詳細資料
<font color=#0000FF> In [7]: </font>

```sql
SELECT 
    students.id AS 學生編號,
    students.name AS 姓名,
    class_list.name AS 班級,
    teachers.name AS 班級老師,
    students.gender AS 性別,
    students.age AS 年齡    
FROM students
INNER JOIN class_list ON students.class_id = class_list.id
INNER JOIN teachers ON students.teacher_id = teachers.id;
```
>INNER JOIN 用於將{ }表與{ }表連接起來
>ON students.class_id = class_list.id表示連接的條件是students表中的class_id欄位與class_list表中的id欄位相等。
>ON students.teacher_id = teachers.id表示連接的條件是students表中的teacher_id欄位與teachers表中的id欄位相等。

<font color=#FF0000> Query Result [7]: </font>
![SELECT FORM INNER JOIN ON](https://firebasestorage.googleapis.com/v0/b/pictureforu-3b8e9.appspot.com/o/2026Node-js-camp-week0-task-2-SQL-Qurety-Result%2F2026-week0-sql-02.png?alt=media&token=0151f518-0bfe-447a-a1a5-35287717ac93)

## Q3：小孩的家庭歸類資料庫，父母資料一直重複實在討厭！

<table>
  <tr>
    <th>小孩編號</th>
    <th>姓名</th>
    <th>父母名稱</th>
    <th>父母電話</th>
    <th>父母性別</th>
  </tr>
  <tr>
    <td>1</td>
    <td>小明</td>
    <td>王大祥</td>
    <td>0973254254</td>
    <td>男</td>    
  </tr> 
  <tr>
    <td>2</td>
    <td>小華</td>
    <td>王曉如</td>
    <td>0955717855</td>
    <td>女</td>    
  </tr> 
  <tr>
    <td>3</td>
    <td>小美</td>
    <td>王大祥</td>
    <td>0973254254</td>
    <td>男</td>    
  </tr> 
  <tr>
    <td>4</td>
    <td>小強</td>
    <td>王曉如</td>
    <td>0955717855</td>
    <td>女</td>    
  </tr>
    <tr>
    <td>5</td>
    <td>小智</td>
    <td>王大祥</td>
    <td>0973254254</td>
    <td>男</td>    
  </tr>
</table>

重複的父母資料可以通過將父母信息獨立出來，建立一個單獨的父母資料表來解決。
然後使用關聯鍵（如父母ID）將小孩表和父母表關聯起來。這樣可以減少數據冗餘，並提高數據庫的可維護性和一致性。

<font color=#0000FF> In [8]: </font>

```sql
BEGIN;

CREATE TABLE parents (
    id SERIAL PRIMARY KEY,
    name VARCHAR(50),
    phone VARCHAR(15),
    gender VARCHAR(10)    
);

INSERT INTO parents(name, phone, gender)
VALUES
    ('王大祥', '0973254254', '男'),
    ('王曉如', '0955717855', '女');

ALTER TABLE students
ADD COLUMN parent_id INTEGER,
ADD CONSTRAINT fk_parent_id
FOREIGN KEY (parent_id) REFERENCES parents(id);

UPDATE students 
SET parent_id = 1
WHERE id IN (1, 3, 5); -- 設定 id 為 1,3,5 學生的 parent_id = 1 (王大祥)

UPDATE students 
SET parent_id = 2
WHERE id IN (2, 4); -- 設定 id 為 2, 4 學生的 parent_id = 2 (王曉如)

COMMIT;
```
>Q3 的父母對應關係不規則（同一位父母對應多個小孩），無法用單一 `UPDATE` 覆蓋，保留兩行 `UPDATE` 搭配 `IN` 是最清楚的寫法。
>電話號碼使用 `VARCHAR` 而非 `INTEGER`，是為了保留開頭的 `0`；若存成數字，`0973...` 會變成 `973...`。

使用 INNER JOIN 合併資料表查詢學生與家長的詳細資料
<font color=#0000FF> In [9]: </font>

```sql
SELECT 
    students.id AS 小孩編號,
    students.name AS 學生姓名, 
    parents.name AS 父母名稱, 
    parents.phone AS 父母電話, 
    parents.gender AS 父母性別
FROM students
INNER JOIN parents ON parents.id = students.parent_id;
```
>通過INNER JOIN將students表與parents表聯接，使用students.parent_id和parents.id作為關聯條件。這樣可以列出每個學生及其對應的父母信息。

<font color=#FF0000> Query Result [9]: </font>
![SELECT AS FROM INNER JOIN ON](https://firebasestorage.googleapis.com/v0/b/pictureforu-3b8e9.appspot.com/o/2026Node-js-camp-week0-task-2-SQL-Qurety-Result%2F2026-week0-sql-03.png?alt=media&token=d35e7a51-8ea8-4839-9a9a-8f8d9f7b48f5)

## 回顧
在本節，我們練習了：
一、規劃外來鍵與資料表拆分
二、主鍵設定方式以及ID 自動遞增方法
三、搭配 Where 條件，合併資料表查詢
四、使用 inner join，合併資料表查詢

五、與2024後端體驗營時練習相比，這次額外套用了以下改善：
- `gender` 欄位加上 `CHECK (gender IN ('男', '女'))` 約束，防止輸入非預期值
- Q2 的五行獨立 `UPDATE` 合併為一行，利用 `teacher_id = id` 的規律
- Q2 In [10] 補上遺漏的分號
- 批次建表與插入資料統一包在 `BEGIN; ... COMMIT;` 裡